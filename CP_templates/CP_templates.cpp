// TODO add LCT store type: point update + path query + no reroot (no propagation at all)
// Then another type of link is needed (where future child must be root)
// Also maybe use splay instead of expose in more places where it is possible (in new store type)

#include <iostream>

using namespace std;

int main()
{
    return 0;
}
